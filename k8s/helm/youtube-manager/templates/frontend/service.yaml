apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    {{- include "youtube-manager.labels" . | nindent 4 }}
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
